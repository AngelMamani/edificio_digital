@page
@model edificio_digital.Pages.LoginModel
@using edificio_digital.Models.Common
@{
    Layout = AppConstants.Layouts.Login;
    ViewData["Title"] = "Iniciar sesión";
}

<div class="login-shell">
    <div class="login-backdrop" aria-hidden="true"></div>

    <div class="login-content">
        <header class="login-brand">
            <span class="login-brand-mark" aria-hidden="true">ED</span>
            <div>
                <span class="login-brand-title">Edificio digital</span>
                <span class="login-brand-tagline">Acceso institucional</span>
            </div>
        </header>

        <div class="login-card">
            <div class="login-card-accent" aria-hidden="true"></div>
            <h1 class="login-title">Iniciar sesión</h1>
            <p class="login-subtitle">Ingresa con tus credenciales. El sistema te redirigirá al panel de tu rol.</p>

            <form method="post" class="login-form mt-4">
                <div class="form-field">
                    <label asp-for="LoginInput.Email" class="login-label"></label>
                    <input asp-for="LoginInput.Email" type="email" autocomplete="username" class="form-control login-input" placeholder="nombre@ejemplo.edu.pe" />
                    <span asp-validation-for="LoginInput.Email" class="text-danger small d-block mt-1"></span>
                </div>

                <div class="form-field">
                    <label asp-for="LoginInput.Contrasena" class="login-label"></label>
                    <input asp-for="LoginInput.Contrasena" type="password" autocomplete="current-password" class="form-control login-input" placeholder="Tu contraseña" />
                    <span asp-validation-for="LoginInput.Contrasena" class="text-danger small d-block mt-1"></span>
                </div>

                <button type="submit" class="btn login-button w-100">Ingresar</button>
            </form>

            @if (!string.IsNullOrWhiteSpace(Model.Message))
            {
                <div class="login-alert alert-danger mt-3" role="alert">
                    @Model.Message
                </div>
            }

            <p class="login-hint mt-3">
                Usuario inicial: <code>admin@edificiodigital.com</code> · contraseña <code>admin</code>
            </p>
        </div>

        <p class="login-foot">
            <a asp-page="@AppConstants.Pages.Home">← Volver al inicio</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
